{
    use NethServer::Password;
    our $cdrdb_pwd = NethServer::Password::store('NethvoiceDBPasswd') || die('Could not generate asterisk cdr database password!');
    our $ctidb_pwd = NethServer::Password::store('CTIDBPasswd') || die('Could not generate nethcti database password!');
    our $phonebookdb_pwd = NethServer::Password::store('PhonebookDBPasswd') || die('Could not generate phonebook database password!');
    return "";
}
\{
