{
    my $http_proto = ${'nethcti-server'}{'Protocol'} || 'http';
    my $url = ${'nethcti-server'}{JabberUrl} || "${http_proto}://${SystemName}.${DomainName}/http-bind";
    $OUT = '{ "url" : "' . $url . '", "domain": "'. $DomainName .'" }';
}
