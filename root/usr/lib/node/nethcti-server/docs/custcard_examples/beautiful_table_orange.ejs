<style type="text/css">
.block {
    overflow: auto;
    -webkit-transition: height 300ms linear;
    -moz-transition: height 300ms linear;
    -o-transition: height 300ms linear;
    transition: height 300ms linear;
}

.blockOrange {
    border: 2px solid #b14e06;
}

#imgOrange {
    margin-left: -15px;
}

.tableName {
    cursor: pointer;
    position: absolute;
    right: 0;
    top:0;
    width: 100%;
    height: 100%;
    padding-top: 11px;
    text-align: center;
}

.tableRuleOrange {
    width: 100%;
   border-collapse: collapse;
   padding: 0px !important;
   height: auto !important;
}

.tableElementOrange {
   font-size: 14px;
   text-align: left;
}

.customThOrange {
   word-break: normal;
   min-width: 70px;
   text-align: center;
   padding: 13px 11px;
   font-weight:700;
   background-color: #fccba7;
   color:black;
   border: 1px solid #a39f9f;
}

.customTdOrange {
   word-break: normal;
   min-width: 70px;
   text-align: center;
   padding: 11px 20px 11px 20px;
   border: 1px solid #a39f9f;
}

tr:nth-child(even){
   background-color: #f2f2f2;
}

tr:nth-child(odd){
   background-color: white;
}

.nameCardOrange{
   clear: both;
   margin-top:10px;
   width:10%;
   color: black;
   background-color: #fccba7;
   border-top:2px solid #b14e06;
   border-left:2px solid #b14e06;
   border-right:2px solid #b14e06;
   min-width:137px;
   min-height: 20px;
   font-weight:700;
   position: relative;
   padding:11px;
   padding-left:30px !important;
}

.nameCardOrange:hover{
   clear: both;
   margin-top: 10px;
   width:10%;
   color: white;
   background-color: #b14e06;
   border-top:2px solid #b14e06;
   border-left:2px solid #b14e06;
   border-right:2px solid #b14e06;
   min-width:137px;
   min-height: 20px;
   font-weight:700;
   position: relative;
   padding:11px;
   padding-left:30px !important;
}

.breakText {
   display: inline-block;
   word-break: break-all;
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
}
</style>

<% if(results.length==0){ %>
        <table width="50%" cellpadding="5" ><tr><td><h3></h3></td></tr> <tr><td>Spiacente, nessun dato presente.</td></tr></table>
<% } else { %>
   
                <div class="nameCardOrange nameCard">
		      <img id = "imgOrange" src="images/sort-down.png" height="10" width="10"/>
                      <label for="showBlockOrange" class="tableName breakText"><%= name %>: <%= results.length %></label>
		</div>
		<div class="block blockOrange">
		    <table class="tableRuleOrange">
		    <tr>
		    <% for (prop in results[0]) {
       			if (prop === "server_address") continue;
		    %>
   		    <th class="tableElementOrange customThOrange"> <%= prop %> </th>
	<%  }%>
</tr>

<% for(var i=0; i<results.length; i++){ %>
  <tr>
     <% for (prop in results[i]) {
            if (prop === "server_address") continue;
	
      %>
              <td class="customTdOrange"><%= results[i][prop] %> </td>
     <%  } %>
  </tr>
                        <% } %>
                </table>
		</div>


<% } %>
