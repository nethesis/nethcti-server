[customer_card_default]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=pbookuser
dbpassword=pbookpass
dbname=phonebook
query=select * from phonebook where homephone like '%$EXTEN' or workphone like '%$EXTEN' or cellphone like '%$EXTEN' or fax like '%$EXTEN'

[customer_card_chiamate]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=
dbpassword=
dbname=asteriskcdrdb
query=select date_format(calldate,'%d/%m/%Y') as date, date_format(calldate,'%H:%i:%S') as time, clid, src, dst, uniqueid,duration,billsec,disposition,if(src='$EXTEN','in','out') as direction from cdr where (src = '$EXTEN' or dst = '$EXTEN') and disposition = 'ANSWERED' order by calldate desc limit 0,10 

[phonebook]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=pbookuser
dbpassword=pbookpass
dbname=phonebook
query=select * from phonebook where name like "%$NAME_TO_REPLACE%" or company like '%$NAME_TO_REPLACE%' or workphone like '%$NAME_TO_REPLACE%' or homephone like '%$NAME_TO_REPLACE%' or cellphone like '%$NAME_TO_REPLACE%' order by name ASC, company ASC

[day_history_call]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=
dbpassword=
dbname=asteriskcdrdb
query=select date_format(calldate,'%d/%m/%Y') as date, date_format(calldate,'%H:%i:%S') as time, clid, src, dst, uniqueid,duration,billsec,disposition from cdr where ( (channel like '%/$EXTEN%' OR dstchannel like '%/$EXTEN%' ) && DATE(calldate)='$DATE' )

[current_week_history_call]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=
dbpassword=
dbname=asteriskcdrdb
query=select date_format(calldate,'%d/%m/%Y') as date, date_format(calldate,'%H:%i:%S') as time, clid, src, dst, uniqueid,duration,billsec,disposition from cdr where ( (channel like '%/$EXTEN%' OR dstchannel like '%/$EXTEN%' ) && ( DATE(calldate)>=(DATE_SUB(CURDATE(), INTERVAL DAYOFWEEK( CURDATE() )-2 DAY)) )  )

[current_month_history_call]
dbhost=localhost
dbport=/var/lib/mysql/mysql.sock
dbtype=mysql
dbuser=root
dbpassword=
dbname=asteriskcdrdb
query=select date_format(calldate,'%d/%m/%Y') as date, date_format(calldate,'%H:%i:%S') as time, clid, src, dst, uniqueid,duration,billsec,disposition from cdr where ( (channel like '%/$EXTEN%' OR dstchannel like '%/$EXTEN%' ) && ( DATE(calldate)>=(DATE_SUB(CURDATE(), INTERVAL DAYOFMONTH( CURDATE() )-1 DAY)) )  )
